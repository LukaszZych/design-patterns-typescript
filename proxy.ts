interface Human {  sayHello();}// Any class that implements the interfaceclass Fred implements Human {  sayHello() {    console.log('Cześć, to ja Fred!');  }}// Proxy which will not allow to use the method unless the password is correctclass Proxy implements Human {  private fred: Human;  private password: string;  constructor(haslo: string) {    this.password = haslo;  }  private createAnInstance() {    this.fred = new Fred();  }  sayHello() {    if (this.password === 'extremlySecret') {      this.createAnInstance();      this.fred.sayHello();    } else {      console.log('Incorrect password');    }  }}// How to use:const pass = 'extremlySecret';const proxy = new Proxy(pass);proxy.sayHello();