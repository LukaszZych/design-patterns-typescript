// Almost the same as state// Interfaceinterface Saveable {  saveData(data: string);}// Statesclass SaveAsTxt implements Saveable {  saveData(data: string) {    console.log('zapis danych jako txt: ', data);  }}class SaveAsPdf implements Saveable {  saveData(data: string) {    console.log('zapis danych jako pdf: ', data);  }}// Concrete class which changes it's behaviourclass Save {  private strategy: Saveable;  constructor(strategy: Saveable) {    this.strategy = strategy;  }  saveText(data: string) {    this.strategy.saveData(data);  }}let save: Save = new Save(new SaveAsTxt());save.saveText('Siabada');save.saveText('Elo elo');save = new Save(new SaveAsPdf());save.saveText('Fooooo');