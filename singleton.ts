export class Singleton {  // a variable to keep the instance of the singleton, in some cases you can just use = new Singleton()  private static singleton: Singleton;  // private constructor so you can't create an instance  private constructor() {}  // method which returns a singleton instance  public static getInstance(): Singleton {    if (!Singleton.singleton) {      Singleton.singleton = new Singleton();    }    return Singleton.singleton;  }  public sayHello() {    console.log('Hello!');  }}// How to use:const logowanie: Singleton = Singleton.getInstance();logowanie.sayHello();