// Main interface with clone methodinterface PersonPrototype {  clone(): PersonPrototype;}// Class which implements cloningclass Student implements PersonPrototype {  name: string;  constructor(name: string) {    this.name = name;  }  // method to clone the current object  clone(): Student {    return new Student(this.name);  }  introduceYourself(): void {    console.log(`Hello my name is ${this.name}`);  }}// How to use:let franek = new Student('Franek');franek.introduceYourself();let grzesiek = franek.clone();grzesiek.introduceYourself();grzesiek.name = 'Grzesiek';grzesiek.introduceYourself();